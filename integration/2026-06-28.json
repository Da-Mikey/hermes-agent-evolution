{
  "date": "2026-06-28",
  "merged": [],
  "skipped": [
    {
      "pr": "19",
      "reason": "CONFLICTING — branch is 599 commits behind main with merge conflicts in 100 files (mergeStateStatus: DIRTY). Actual feature change (add exit codes 126/127/128 to terminal_tool.py) is correct but the branch includes 66 upstream sync commits causing formatting-only conflicts. Needs fresh rebase or re-implementation from current main."
    },
    {
      "pr": "20",
      "reason": "CONFLICTING — branch is 599 commits behind main with merge conflicts in 100 files (mergeStateStatus: DIRTY). Actual feature change (replace '(empty)' placeholder text in conversation_loop.py) is correct but the branch includes 66 upstream sync commits causing formatting-only conflicts. Needs fresh rebase or re-implementation from current main."
    }
  ],
  "note": "Neither PR has ever had CI run (no checks reported). Both are evolution/issue-* branches from Da-Mikey with valid issue references (closes #18, closes #16) but are too far behind main to merge safely. The diff is 100 files each because of historical sync commits — the actual feature changes are minimal. Recommend fresh re-implementation branches from current main HEAD."
}
